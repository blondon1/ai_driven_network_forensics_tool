package reporting

import (
	"fmt"
	"github.com/google/gopacket"
)

func GenerateReport(packet gopacket.Packet) {
	// Placeholder for report generation logic
	fmt.Println("Generating report for packet:", packet)
}
